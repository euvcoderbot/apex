# APEX DATA

Static F1 telemetry comparison prototype.

Open `index.html` in a browser. It includes session controls, multi-driver lap selection, stints, reference-lap switching, finish-line deltas, sector timing rows, and synchronized telemetry charts using representative data.
